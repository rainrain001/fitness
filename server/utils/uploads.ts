// Max size for a food picture upload. Enforced on the server here and mirrored
// on the client (see FoodDialog.vue) so the user gets immediate feedback.
export const MAX_FOOD_IMAGE_BYTES = 1024 * 1024 // 1 MB
